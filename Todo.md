# Тест REG_VIDEO NO20260707-145101 (после фикса)

## Результат (interval=1с, БД E2E_FIX3_*)
Попали точно: `9036СС45`, `Н767АК125`, `О447ЕН125`, `Р789ОН125`, `Х034ХА125`  
Почти: `В713ВВ125` → OCR даёт `Н713ВВ125` (путаница В/Н)  
Квадратный: `Т314ХС125` — ловится (2-line + RapidOCR)

## Что сделано
- военный формат + invert ROI + фикс Е9036СС45 / У036СС45→9036СС45
- всегда crop+full+ROI (не блокировать ROI кривым crop)
- сохранение по стволу номера (одно лучшее чтение за проезд)
- двухстрочные/квадратные: `NOMEROFF_TWO_LINE=1`, eu_2lines + RapidOCR на разжатом кропе


## Что доделать/поправить
- [x] нет авто в фотоинформации, а номер в БД есть — строка в `SPR_SP_FOTO_TABLE`
      теперь вставляется всегда, кадр берётся по максимальной площади bbox,
      а кроп для `F_PLATE` — из оригинального кадра по обратно отображённому bbox
      (раньше туда мог уехать негатив или растянутый ROI)
- [x] дата, время не распознаются — время кадра = начало записи + `TimeSec`.
      Начало берётся из имени файла регистратора, затем `ffprobe creation_time`,
      затем дата файла; OSD служит перекрёстной проверкой и переопределяет
      начало, если оно взято из даты файла. `DateTime.Now` не используется
- [ ] сложность запуска и контроля — может docker, может быть compose, может монолит?
      (отложено по решению: сейчас `start-app.bat` + `prepare-offline.bat`)
- [x] подумать над другой моделью распознавания — реализовано тремя шагами:
      голова `ru_military` подключается, если вес есть локально;
      авторазметка своего материала для дообучения (`tools/export_ocr_dataset.py`);
      локальный VLM — только арбитром спорных треков (`NOMEROFF_VLM`), не детектором
